# Scripts (placeholder)

Utility scripts such as build helpers, asset packers, and dev helpers will go here.

Examples:
- `dev-server.ps1` — run dev server with watchers
- `build-assets.ps1` — pack and optimize images/sounds

Add implementation scripts later.
